<UserControl x:Class="StockAnalysis.UI.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockAnalysis.UI.Controls"
             xmlns:nav="clr-namespace:StockAnalysis.UI.State.Navigators"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                  Text="STOCK ANALYSIS"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <ComboBox Width="200" />
            <Button Content="Tìm Kiếm" Margin="10,0,0,0"/>
        </StackPanel>
        <GroupBox Grid.Row="2" Background="{StaticResource Primary500}" Foreground="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Công ty cổ phần ..." />
                <TextBlock Grid.Row="1" Text="Ngành: BDS" />
            </Grid>
        </GroupBox>
         <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <RadioButton Grid.Column="0"
                         Content="Giao Dịch" Command="{Binding UpdateCurrentViewModelCommand}" 
                         CommandParameter="{x:Static nav:ViewType.Stock}"/>
            <RadioButton Grid.Column="1"
                         Content="Tài Chính"
                         Command="{Binding UpdateCurrentViewModelCommand}"
                         CommandParameter="{x:Static nav:ViewType.Finance}" />
            <RadioButton Grid.Column="2"
                         Content="So Sánh"
                         Command="{Binding UpdateCurrentViewModelCommand}"
                         CommandParameter="{x:Static nav:ViewType.Comparison}" />
        </Grid>
    </Grid>
</UserControl>
